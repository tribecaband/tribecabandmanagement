import React from 'react'

// SessionAlert is now simplified since Supabase handles session refresh automatically
// This component can be used in the future for other session-related notifications
export default function SessionAlert() {
  // Currently disabled - Supabase handles token refresh automatically
  // If we need session alerts in the future, we can implement them here
  return null
}